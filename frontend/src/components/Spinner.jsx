/**
 * Spinner component for displaying a spinning wheel while the results are loading.
 *
 * @returns {JSX.Element} A spinner element indicating loading state.
 */

export default function Spinner() {
  return (
    <div style={{ marginTop: "20px" }}>
      🔄 Buscando rutas...
    </div>
  )
}